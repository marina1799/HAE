import { NativeBaseProvider, Fab, Text, Flex, Icon } from "native-base";
import { Ionicons } from "@expo/vector-icons";

const CreateRecipes = ({ route, navigation }) => {

  return (
    <NativeBaseProvider>

    </NativeBaseProvider>
  );
};

export default CreateRecipes;
